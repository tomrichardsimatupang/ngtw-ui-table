import { UiTableColDirective } from './ui-table-col.directive';

describe('UiTableColDirective', () => {
  it('should create an instance', () => {
    const directive = new UiTableColDirective();
    expect(directive).toBeTruthy();
  });
});
