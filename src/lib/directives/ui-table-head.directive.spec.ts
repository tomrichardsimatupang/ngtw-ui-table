import { UiTableHeadDirective } from './ui-table-head.directive';

describe('UiTableHeadDirective', () => {
  it('should create an instance', () => {
    const directive = new UiTableHeadDirective();
    expect(directive).toBeTruthy();
  });
});
