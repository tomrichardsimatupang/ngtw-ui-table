import { UiTableDataDirective } from './ui-table-data.directive';

describe('UiTableDataDirective', () => {
  it('should create an instance', () => {
    const directive = new UiTableDataDirective();
    expect(directive).toBeTruthy();
  });
});
